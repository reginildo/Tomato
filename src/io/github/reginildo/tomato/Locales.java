package io.github.reginildo.tomato;

import java.util.Locale;

class Locales {
    static Locale localeDefault = Locale.getDefault();
    static Locale locale_pt_BR = new Locale("pt", "BR");
    static Locale locale_en_US = new Locale("en", "US");
    static Locale locale_tlh = new Locale("tlh");
}
